{
  "name": "scorched-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Scorched Mobile — HTML5 Canvas game wrapped with Capacitor for native iOS (TestFlight).",
  "scripts": {
    "build": "node scripts/build-web.js",
    "cap:sync": "cap sync ios",
    "cap:add-ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
